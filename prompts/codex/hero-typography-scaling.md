Update the hero section typography to better match the reference website provided earlier.

Changes required:

1. Increase the size of the "DESIGNER" text so it becomes much larger and more dominant, similar to the reference design.  
2. The text should span wider across the hero section and remain positioned behind the subject image.
3. Ensure the subject still appears in front of the text using proper layering (z-index).

4. Adjust the position of the small "FASHION" text so it aligns correctly with the new size of "DESIGNER".
5. "FASHION" should remain positioned near the top-left of the "DESIGNER" text and maintain proper spacing so it looks visually balanced.

Layout rules:
- Keep the subject centered.
- Keep "DESIGNER" behind the subject.
- Maintain responsive behavior across screen sizes.
- Ensure typography scales correctly on desktop, tablet, and mobile.

Important:
Do NOT change anything else in the website.
Do NOT modify other sections, styles, assets, or functionality.
Only adjust the typography size and positioning related to "DESIGNER" and "FASHION".

Output:
Provide only the minimal HTML and CSS updates required.
