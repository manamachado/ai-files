Update the existing website to make it **fully mobile responsive** while keeping the current design intact.

Important:
Only improve **mobile and tablet responsiveness**.
**Do NOT change the design, colors, typography, content, layout structure, animations, or sections.** The desktop version must remain exactly the same.

Requirements:

1. Ensure the website works properly on **mobile screen sizes (320px – 768px)** and **tablet screens (768px – 1024px)**.

2. Implement responsive styles using **CSS media queries**.

3. Navigation:

   * Convert the desktop navigation menu into a **mobile hamburger menu**.
   * The menu should open as a **full-screen or slide-in menu** on smaller screens.
   * Keep the "Let's Talk" button accessible.

4. Hero Section:

   * Stack hero content vertically for mobile.
   * Reduce heading sizes while maintaining typography hierarchy.
   * Ensure the hero image scales properly and does not overflow.

5. Section Layouts:

   * Convert **two-column layouts into single-column layouts** on mobile.
   * Maintain consistent spacing and alignment.
   * Ensure sections remain visually balanced.

6. Image Grids:

   * Change multi-column image grids into **1 or 2 columns on smaller screens**.
   * Images should scale to **width: 100%** without distortion.

7. Carousel / Portfolio:

   * Ensure the carousel works smoothly on touch devices.
   * Enable **horizontal swipe scrolling**.

8. Typography:

   * Adjust heading and paragraph sizes for better readability on small screens.
   * Maintain the same font families.

9. Spacing:

   * Adjust padding and margins for smaller screens.
   * Prevent text from touching screen edges.

10. Performance:

* Ensure images scale properly and load efficiently on mobile devices.

Goal:
The website should look **clean, readable, and easy to navigate on mobile devices** while preserving the exact visual design and behavior of the desktop version.

Do not modify any existing desktop styling — only add responsive improvements for mobile and tablet screens.
