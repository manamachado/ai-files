Update the existing website implementation to fix the layout dimensions and make the site properly follow standard desktop resolutions.

Important:
Only adjust the **layout dimensions and responsiveness**.
**Do NOT change the design, colors, typography, structure, animations, sections, or any existing content.** Everything else must remain exactly the same.

Requirements:

1. The website should be **full-width and full-screen**, not inside a narrow centered container.
2. Remove or modify any outer wrapper that limits the website width or creates large margins on the left and right.
3. The layout should properly support **desktop resolutions up to 1920px width**.
4. Use a responsive container system:

   * content sections: max-width between **1400px–1600px**
   * section backgrounds: **width: 100%** so they span the full screen
5. Dark and light background sections should **stretch across the entire browser width**.
6. Ensure the **hero section fills the full viewport height (100vh)** on desktop screens.
7. Images and grids should scale proportionally on larger displays without breaking the layout.
8. Adjust spacing and alignment so the design looks balanced on **1440px, 1600px, and 1920px** screens.
9. Maintain responsiveness for **tablet and mobile screens**.

Again, **do not modify any visual design elements or content** — only fix the **dimensions and full-screen layout behavior**.
