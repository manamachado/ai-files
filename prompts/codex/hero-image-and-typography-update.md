Update the hero section based on the following design changes.

Changes required:

1. Remove the monochrome / grayscale effect from the subject image so the image appears in full color.

2. Make the subject image slightly larger while keeping her centered on the page.

3. Only keep the word "DESIGNER" as the large background text behind the subject.
Remove any other background words.

4. Increase the size of the "DESIGNER" text so it is significantly larger than the current version and clearly visible behind the subject.

5. Add a smaller text "FASHION" and position it at the top-left corner of the large "DESIGNER" text.

Layout rules:
- The subject must stay in front of the text.
- "DESIGNER" should remain behind the subject.
- "FASHION" should sit slightly above and to the left of the word "DESIGNER".
- Maintain proper layering using CSS positioning and z-index.
- Keep the design responsive.

Important:
Do NOT change anything else in the website.
Do NOT modify other sections, styles, assets, or functionality.
Only update the hero section elements necessary to achieve these changes.

Output:
Provide only the minimal HTML and CSS changes required.
